---
interface Props {
  title: string;
  url?: string;
}
const { title, url } = Astro.props;
---
<div class="card" style="padding: 1.25rem;">
  <h3 style="font-size: 1.1rem; margin-bottom: 0;">
    {url ? <a href={url} class="no-underline">{title} &rarr;</a> : <span>{title}</span>}
  </h3>
</div>
